name: Build Vivo T4 Kernel
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Actions
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential cpio curl flex git libssl-dev libncurses5-dev zlib1g-dev libelf-dev dwarves wget unzip xz-utils tar

      - name: Download Source from GitHub Release
        run: |
          wget -O kernel.tar.gz "https://github.com/abdulla-li/Android-Kernel-Builder/releases/download/Build/android_16.0_kernel_SM7635.tar.gz"

      - name: Extract Source
        run: |
          mkdir source
          tar -xzvf kernel.tar.gz -C source

      - name: Download Official Google Toolchain
        run: |
          # Vivo didn't include the toolchain, so we download it directly from Google.
          # Using sparse-checkout to grab ONLY the exact version we need, saving time and space.
          echo "Cloning official Clang r487747c..."
          git clone --filter=blob:none --no-checkout https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 toolchain
          cd toolchain
          git sparse-checkout set clang-r487747c
          git checkout main || git checkout master
          echo "Toolchain downloaded successfully."

      - name: Build Kernel
        run: |
          # 1. Point the build system to the Google toolchain we just downloaded
          export CLANG_PATH=$PWD/toolchain/clang-r487747c
          export PATH=$CLANG_PATH/bin:$PATH
          
          # Verify the linker is now available
          ld.lld --version
          
          export ARCH=arm64
          export SUBARCH=arm64
          export CC=clang
          export LLVM=1
          export LLVM_IAS=1
          export CROSS_COMPILE=aarch64-linux-android-
          
          # 2. Find the exact config file in the intact tree
          CONFIG_PATH=$(find $PWD/source -path "*/arch/arm64/configs/gki_defconfig" | head -n 1)
          echo "Found config at: $CONFIG_PATH"
          
          # 3. Strip the known suffix to get the pure kernel root
          KERNEL_DIR=${CONFIG_PATH%/arch/arm64/configs/gki_defconfig}
          echo "Navigating to Kernel Root: $KERNEL_DIR"
          cd "$KERNEL_DIR"
          
          # 4. Build vmlinux and Image
          make O=out gki_defconfig
          make -j$(nproc) O=out vmlinux Image
          
          # 5. Prepare artifacts for upload
          mkdir -p $GITHUB_WORKSPACE/upload_artifacts
          cp out/vmlinux $GITHUB_WORKSPACE/upload_artifacts/ || true
          cp out/include/generated/asm-offsets.h $GITHUB_WORKSPACE/upload_artifacts/ || true

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-build-output
          path: upload_artifacts/
          
